{
  "version": "0.1.0",
  "notes": "Zero-point achievements. Badges never contribute to the score, never gate attestation, and are deliberately kept out of spec.json — that file is the scoring contract the engine and the EAS attestation are built from.",
  "badges": [
    {
      "slug": "talent_token_launched",
      "name": "Launched a Talent Token",
      "description": "Deployed a personal Talent Token through Talent Protocol's v1 TalentFactory.",
      "source": "allowlist",
      "allowlist": "talent-token-launched",
      "notes": "The Celo factory never populated the talent → token direction: talentsToTokens returns 0x0 even for wallets whose createTalent succeeded, and hasTalentToken reverts. Only Polygon answers a live per-wallet read, and it holds 20 of the 564 talents. So the badge matches against the factories' TalentCreated history instead — public chain data, regenerated by scripts/build-talent-token-allowlist.mjs, frozen because both factories have been dormant since July 2023."
    },
    {
      "slug": "build_contributor",
      "name": "$BUILD Contributor",
      "description": "Took part in BUILD on Base — donated to the airdrop, or registered for pay-it-forward.",
      "source": "rpc",
      "method": "positive_uint_call",
      "call": "donated(address)",
      "contracts": [
        { "name": "BUILD Airdrop", "chain": "base-mainnet", "address": "0x556e182ad2b72f5934C2215d6A56cFC19936FdB7" }
      ],
      "snapshot": true,
      "notes": "Checked both ways and earned either way. The onchain donated() accumulator only sees wallets that donated directly. The snapshot carries every wallet registered for BUILD pay-it-forward — both the social and the custody side, since custody wallets are not linked accounts and appear nowhere else — expanded profile-wide so any wallet a person owns matches. Read it as participation in BUILD, not proof of a contribution amount: a registration earns the badge without a recorded donation. The export also reads the stored build_contribution data point, but that yields nothing today: the credential row was deleted when it was retired from the 2025 set, and data points cascade with it. The query stays in place for the day it returns."
    },
    {
      "slug": "builder_score_100",
      "name": "Builder Score 100+",
      "description": "Reached a Builder Score of 100 or more on Talent Protocol.",
      "source": "snapshot",
      "notes": "Builder Score lives in Talent Protocol's database with no permissionless read, so this badge is a dated export rather than a live check."
    },
    {
      "slug": "builder_rewards_earned",
      "name": "Earned Builder Rewards",
      "description": "Received a completed Builder Rewards payout from a Talent Protocol grant.",
      "source": "snapshot",
      "notes": "Rewards are paid from a per-grant wallet through a per-grant multisend contract, so there is no stable distributor address to check onchain."
    }
  ]
}
